{
  "name": "community-center-client",
  "private": true,
  "scripts": {
    "start": "npm start --prefix my-app",
    "build": "npm run build --prefix my-app",
    "test": "npm test --prefix my-app",
    "install:app": "npm install --prefix my-app"
  }
}
