FILE: 📘 אפיון מערכת.docx

📘 אפיון מערכת – מרכז קהילתי (Full Stack System)🧠 1. ארכיטקטורה כלליתהמערכת בנויה לפי Clean Architecture:React (Frontend)        ↓ASP.NET Core API (Controllers)        ↓Middleware (JWT + Authorization)        ↓Application Layer (Services + Business Logic)        ↓Infrastructure Layer (Repositories + EF Core)        ↓Database (SQL Server)🔐 2. מערכת משתמשים ואבטחה (Authentication & Authorization)🟢 React – משתמשיםדפים:RegisterPage LoginPage פונקציות:הרשמהvalidateForm() handleRegister() שליחה:POST /auth/registerהתחברותhandleLogin() saveTokenToStorage() שליחה:POST /auth/loginניהול טוקן:save token to localStorage attach token to every request Authorization: Bearer <token>🖧 Server – AuthControllerPOST /auth/registerPOST /auth/loginService – AuthServiceפונקציות:Register(RegisterDto) Login(LoginDto) GenerateJwtToken(user) ValidatePassword() לוגיקה:Register:בדיקת אימייל קיים בדיקת תעודת זהות קיימת Hash Password יצירת משתמש שמירה ל-DB Login:שליפת משתמש לפי Email בדיקת סיסמה (BCrypt) יצירת JWT Token החזרת משתמש + טוקן 🔐 JWT Authenticationיצירת טוקן:Claims:UserId Email Role (User / Admin) Middleware – Authenticationבדיקת Authorization Header בדיקת תקינות טוקן חילוץ UserId ל-HttpContext Middleware – Authorizationבדיקת Role חסימת גישה ל-Admin בלבד 🧱 Roles במערכתUser → משתמש רגיל Admin → מנהל מערכת 🏠 3. דף בית (Home Page)🟢 Reactפונקציות:fetchUpcomingEvents() fetchLatestNews() API:GET /events/upcomingGET /news/latestתצוגה:3 אירועים קרובים חדשות אחרונות 🎓 4. חוגים (Courses Module)🟢 Reactעמוד:CoursesPageפונקציות:טעינת חוגיםfetchCourses() GET /coursesהרשמה לחוגhandleRegister(courseId) POST /courses/registerסינון:filterByCategory() filterByAudience() 🖧 ServerControllerGET /coursesPOST /coursesPOST /courses/registerDELETE /courses/:idService – CourseServiceפונקציות:GetAllCourses() RegisterToCourse(userId, courseId) CheckIfAlreadyRegistered() Business Logic:משתמש לא יכול להירשם פעמיים בדיקת קיום חוג שמירת CourseRegistration Repository:GetCourses() AddCourse() AddRegistration() 🎟️ 5. אירועים (Events Module)🟢 ReactPage:EventsPageפונקציות:fetchEvents() handleRegister(eventId, quantity) API:GET /eventsPOST /events/register🖧 ServerController:GET /eventsPOST /eventsPOST /events/registerDELETE /events/:idService – EventServiceפונקציות:GetEvents() RegisterToEvent() CheckCapacity() לוגיקה עסקית:בדיקת מקסימום משתתפים חישוב כמות כרטיסים שנמכרו חסימת הרשמה אם מלא Repository:GetEventById() AddEvent() AddRegistration() 📰 6. חדשות (News Module)🟢 ReactfetchNews() GET /news🖧 ServerGET /newsPOST /news (Admin)DELETE /news/:idService:GetNews() CreateNews() DeleteNews() 📩 7. צור קשר (Contact Module)🟢 ReacthandleSubmitContact() POST /contact🖧 ServerPOST /contactGET /contact (Admin)PATCH /contact/:id/resolveService:SendMessage() GetMessages() MarkAsResolved() 👤 8. אזור אישי (Profile / Dashboard)🟢 ReactPage:ProfilePageפונקציות:fetchUserProfile() fetchUserCourses() fetchUserEvents() API:GET /users/meGET /users/me/coursesGET /users/me/eventsתצוגה:פרטי משתמש חוגים רשומים אירועים שנרכשו 🧑‍💼 9. מערכת ניהול (Admin Dashboard)🟢 ReactPage:AdminDashboardפונקציות:createCourse() deleteCourse() createEvent() createNews() viewReports() 🖧 ServerPOST /coursesDELETE /courses/:idPOST /eventsDELETE /events/:idPOST /newsGET /reportsהרשאות:רק Admin יכול לגשת 🔐 Middleware Admin Checkif (user.Role != "Admin")    return 403 Forbidden🗄️ 10. Database (מבנה כללי)טבלאות:Subscribers Courses Events CourseRegistrations EventRegistrations News ContactRequests Categories Locations TargetAudiences Employees 🔁 11. סדר עבודה לפיתוח (חשוב מאוד)שלב 1 – Database Designיצירת Entities קשרים (FK) שלב 2 – InfrastructureDbContext Repositories שלב 3 – ApplicationServices Business Logic DTOs שלב 4 – APIControllers JWT Middleware שלב 5 – ReactPages Components API Services שלב 6 – אינטגרציהבדיקות זרימה מלאה תיקוני באגים 🔥 12. מה הופך את הפרויקט למושלם✔ JWT Authentication✔ Middleware (Auth + Role)✔ הפרדה מלאה בין שכבות✔ Business Logic אמיתי✔ הרשמות עם בדיקות✔ מערכת Admin✔ אזור אישי✔ REST API מסודר📌 סיוםזה האפיון המלא של המערכת שלך ברמה של פרויקט גמר מקצועי.